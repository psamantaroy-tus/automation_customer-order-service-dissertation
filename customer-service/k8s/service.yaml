apiVersion: v1
kind: Service
metadata:
  name: customer-service-svc
spec:
  selector:
    app: customer-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  # ClusterIP makes this reachable only within the cluster (order-service uses this)
  type: ClusterIP
